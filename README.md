## Commands to run
Note, all these should be run in workspaces/C4CDemo
### Commands to set up everything
```bash
sudo apt-get update && sudo apt-get install -y cmake
chmod +x build_and_run.sh
```
### Command to build and run program
```bash
./build_and_run.sh
```
